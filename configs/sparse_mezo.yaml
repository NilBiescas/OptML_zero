# Sparse-MeZO (Liu et al. 2024). Magnitude-sparse perturbation mask.
# Implementation: optimizers/sparse_mezo.py — see NUS-HPC-AI-Lab/SparseMeZO.
owner: maria

model:
  name: Qwen/Qwen3.5-0.8B

training:
  seed: 42
  batch_size: 16
  max_steps: 20000
  eval_steps: 500
  num_train: 1000
  num_eval: 1000

optimizer:
  name: SparseMeZO
  kwargs:
    # TODO: fill in hyperparameters from the paper / reference code
