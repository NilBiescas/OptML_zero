# MeZO (Malladi et al. NeurIPS 2023). Vanilla SPSA baseline.
# Implementation: optimizers/mezo.py — see Cheng's `chenghengli` branch for reference.
owner: cheng

model:
  name: Qwen/Qwen3.5-0.8B

training:
  seed: 42
  batch_size: 16
  max_steps: 20000
  eval_steps: 500
  num_train: 1000
  num_eval: 1000

optimizer:
  name: MeZO
  kwargs:
    # TODO: fill in hyperparameters from the paper / reference code
