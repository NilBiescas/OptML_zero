# LOZO (Chen et al. ICLR 2025). Low-rank perturbation U V^T, lazy V resample.
# Implementation: optimizers/lozo.py — see upstream LOZO/large_models reference.
owner: nil

model:
  name: Qwen/Qwen3.5-0.8B

training:
  seed: 42
  batch_size: 16
  max_steps: 20000
  eval_steps: 500
  num_train: 1000
  num_eval: 1000

optimizer:
  name: LOZO
  kwargs:
    # TODO: fill in hyperparameters from the paper / reference code
